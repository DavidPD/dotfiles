#! /usr/bin/env zsh

# TODO: auto copy .example files, without overriding
cp "$HOME/.dotfiles/example configs/zshrc.example" $HOME/.zshrc
cp "$HOME/.dotfiles/example configs/gitconfig.example" $HOME/.gitconfig
